#include "Spawner.h"

Spawner::Spawner(Game* owner, Vector2 pos) : Actor(owner) {
	// set spawner's postion
	//mPosition = pos;
}

Spawner::~Spawner() { }

//void Spawner::OnUpdate(float deltaTime) { }
